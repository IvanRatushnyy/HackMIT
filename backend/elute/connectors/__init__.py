"""Connectors: ToolUniverse (preferred transport) and the direct public APIs (recorded fallbacks).
Every connector returns the same RawRecord shape; nothing downstream knows which delivered a record."""
